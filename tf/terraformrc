plugin_cache_dir = "$HOME/terraform-cache"
plugin_cache_may_break_dependency_lock_file = true
provider_installation {
  filesystem_mirror {
    path    = "$HOME/terraform-cache/"
    #include = ["registry.terraform.io/hashicorp/*"]
  }
}
